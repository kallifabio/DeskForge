Hier legt der Provisioning-Service zur Laufzeit `state.json` ab (Pool-
und Zuweisungs-Zustand, siehe `STATE_FILE_PATH` in `.env`). Diese Datei
wird automatisch erzeugt - nichts manuell hier ablegen. Für Backups
siehe `docs/BACKUP.md`, Abschnitt 4.
